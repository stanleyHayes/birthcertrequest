import Layout from "../../components/layout/layout";
import {Container, Typography} from "@mui/material";

const NotFound = () => {
    return (
        <Layout>
            <Container>
                <Typography variant="h3" align="center">Not Found Page</Typography>
            </Container>
        </Layout>
    )
}

export default NotFound;
